import React, { Fragment } from 'react';
import TopBar from '../top-bar';
import CoursesList from '../courses-list';

const App = () => (
  <Fragment>
    <TopBar />
    <CoursesList />
  </Fragment>
);

export default App;
